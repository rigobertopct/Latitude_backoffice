# Producción en VPS (mismo Nginx para /backoffice y /api)
# Dejar VACÍO para usar URLs relativas /api/... (recomendado con http://IP)
VITE_API_URL=

# Solo si el API está en otro host:
# VITE_API_URL=http://2.24.94.238
